# КУЗНИЦА · ПОДКЛЮЧЕНИЕ ВЕРСТАКА

- Верстак приехал из `mebelchi-3-build/src` — **без его копии движка** (копия = источник расхождений, третий раз платим)
- Канонический движок теперь ОДИН: `../dvizhok/` (контракты — через `../dogovor/shemy/`)

## Что где

- `verstak/forge/` — сессия · превью · purity-тест · модификаторы (рабочее)
- `verstak/three/` — Stage3D · материалы · renderBlock
- `verstak/engine-lokal/` — 6 файлов, которых НЕТ в каноне: `snap` (магнит) ·
  `layoutAdapter` (фикс поворота задней стенки) · `block`+`cutlist` (старое Block-демо,
  не удалять пока новое не работает) · `test_dry_run` · `units`

## Одна задача подключения (для Саидислома)

- [ ] Импорты `../engine/*` в verstak-файлах перевести: канон → `../../dvizhok/*`,
      локальное → `./engine-lokal/*`
- [ ] `npm run kontrol` + purity-тест зелёные → строка в реестр → скан

## Законы (не обсуждаются)

- Мёртвый каркасный код в `forge/session.ts` (DB/35) — удалить при подключении
- Кассеты пишутся в `cartridges/`, верстак кассеты не минует
